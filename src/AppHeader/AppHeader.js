import styles from "./AppHeader.module.css";

const AppHeader = () => (
  <div className={styles.container}>
    <span className={styles.title}>Image gallery</span>
  </div>
);

export default AppHeader;
